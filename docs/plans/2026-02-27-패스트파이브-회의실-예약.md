# 패스트파이브 회의실 디스코드 예약 봇 구현 계획

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 디스코드에서 자연어로 패스트파이브 회의실을 예약하고, 가용 여부 피드백을 받는 n8n 워크플로우를 만든다.

**Architecture:** Discord 봇(입력) → n8n Webhook → Claude LLM(자연어 파싱) → FastFive REST API(로그인/조회/예약) → Discord 웹훅(결과 전송). 기존 "개인 일정 등록" 워크플로우의 Claude Proxy + Whisper STT 패턴을 재활용한다.

**Tech Stack:** n8n, Discord.js (봇 브릿지), Claude API Proxy (`http://10.0.0.1:3456`), Whisper STT (`http://host.docker.internal:8100`), FastFive REST API (`https://api.fastfive.co.kr`)

---

## 사전 지식: FastFive API (Task 1에서 검증 완료)

### 확인된 값
- **memberGroupId:** `351645` (주식회사 군돌이)
- **branchId:** `90` (신논현2호점)
- **memberId:** `144402` (지윤우)
- **spaceCategoryId:** `7` (MeetingRoom)

### 인증
- POST `https://api.fastfive.co.kr/auth/signin` — `{ "application": "web", "email": "...", "password": "..." }`
- 응답: `{ "success": true }` + Set-Cookie (`connect.sid`, `application`)
- 쿠키 기반 세션. 모든 요청에 헤더 `X-Application: web` 필수

### 핵심 예약 API (검증됨)
| 메서드 | 경로 | 필수 파라미터 | 용도 |
|--------|------|---------------|------|
| GET | `/mobile/spaces/reservable` | `spaceCategoryId=7&branchId=90&date=YYYY-MM-DD&startTime=<ISO>&endTime=<ISO>&sort=name&order=asc` | 예약 가능한 회의실 목록 |
| GET | `/mobile/spaces/reservable/{id}` | `spaceCategoryId=7&branchId=90&date=...&startTime=...&endTime=...&memberGroupId=351645` | 특정 회의실 상세 + 해당 시간대 예약 현황 |
| POST | `/mobile/spaceReservations` | body: `{ spaceId, startTime, endTime, memberGroupId }` | 예약 생성 |
| GET | `/mobile/spaceReservations` | `memberGroupId=351645&startDate=...&endDate=...&type=group` | 그룹 예약 목록 |
| DELETE | `/mobile/spaceReservations/{id}` | — | 예약 취소 → `{ success, returnedCredits }` |

### 날짜 형식 (주의)
- **공간 조회 쿼리 (startTime/endTime):** ISO 형식 — `2026-02-28T14:00:00+09:00`
- **예약 생성 body (startTime/endTime):** 로컬 형식 — `"2026-02-28 14:00:00"`
- **예약 목록 쿼리 (startDate/endDate):** 날짜만 — `2026-02-28`

### 회의실 목록 (신논현2호점, 14개)
| 이름 | id | 수용인원 | 층 |
|------|-----|---------|-----|
| 4A | 8109 | 6 | 4 |
| 4B | 8110 | 8 | 4 |
| 5A | 8111 | 6 | 5 |
| 5B | 8112 | 8 | 5 |
| 6A | 8113 | 6 | 6 |
| 6B | 8114 | 6 | 6 |
| 7A | 8115 | 6 | 7 |
| 7B | 8116 | 4 | 7 |
| L1A | 8103 | 14 | L1 |
| L1B | 8104 | 4 | L1 |
| L1C | 8105 | 4 | L1 |
| L1D | 8106 | 10 | L1 |
| L1E | 8107 | 18 | L1 |
| L1F | 8108 | 10 | L1 |

### 핵심 주의사항
- **예약 생성 시 `memberGroupId: 351645` 필수** — 없으면 예약이 그룹에 연결되지 않아 조회/취소 불가
- 공간 목록의 room id 필드는 `id` (not `spaceId`)
- `sort=name&order=asc` 파라미터 없으면 INVALID_PARAMETER
- 30분 = 1 크레딧
- 예약 가능 여부: 공간 상세 API의 `spaceReservations` 배열이 비어있으면 가능

### 공간 카테고리
- meeting-room: spaceCategoryId **7** (= URL `/reserve/7`)

### 기존 인프라 (재활용)
- Claude Proxy: `http://10.0.0.1:3456/v1/chat/completions` (model: `claude-sonnet-4-6`)
- Whisper STT: `http://host.docker.internal:8100/v1/audio/transcriptions`
- 기존 "개인 일정 등록" 워크플로우 (`GHtwwMEAXRRTonI7`) 패턴 참조

---

## Task 0: 사용자 입력 필요 — FastFive 로그인 정보 + Discord 봇 토큰

이 작업을 시작하기 전에 사용자에게 아래 정보를 확인한다:

1. **패스트파이브 로그인 정보** — 이메일, 비밀번호 (n8n credential로 저장)
2. **패스트파이브 memberGroupId** — 신논현 2호점의 그룹 ID (로그인 후 `/mobile/user/memberGroups`로 확인 가능)
3. **패스트파이브 branchId** — 신논현 2호점의 지점 ID (로그인 후 확인)
4. **Discord 봇 토큰** — [Discord Developer Portal](https://discord.com/developers/applications)에서 생성
5. **Discord 채널 ID** — 예약 봇이 동작할 채널
6. **Discord 웹훅 URL** — 해당 채널에 생성한 웹훅 (n8n이 결과 전송에 사용)

---

## Task 1: FastFive API 인증 검증

FastFive API에 로그인하고 세션이 유지되는지 확인한다.

**Step 1: n8n에서 수동 테스트 워크플로우 생성**

n8n MCP로 간단한 테스트 워크플로우를 만든다:
- Manual Trigger → HTTP Request (로그인) → HTTP Request (프로필 조회) → HTTP Request (지점 목록)

```
노드 1: Manual Trigger
노드 2: FastFive Login
  - POST https://api.fastfive.co.kr/auth/signin
  - Body: { "application": "web", "email": "<사용자 이메일>", "password": "<비밀번호>" }
  - Headers: X-Application: web
  - Options: Always Output Data = true
노드 3: Get Profile
  - GET https://api.fastfive.co.kr/mobile/user/profile
  - Headers: X-Application: web
노드 4: Get Member Groups
  - GET https://api.fastfive.co.kr/mobile/user/memberGroups
  - Headers: X-Application: web
```

**Step 2: 실행하여 memberGroupId, branchId 확인**

Run: `n8n_test_workflow` (Manual Trigger)
Expected: 로그인 성공, 프로필 반환, memberGroups에서 신논현 2호점의 memberGroupId와 branchId 확인

**Step 3: 예약 가능 공간 조회 테스트**

```
노드 5: Get Reservable Spaces
  - GET https://api.fastfive.co.kr/mobile/spaces/reservable
  - Query: spaceCategoryId=7, branchId=<확인된 ID>, date=<오늘>, startTime=<예시>, endTime=<예시>
  - Headers: X-Application: web
```

Expected: 회의실 목록 반환 (이름, 수용인원, 층, 시설 태그 등)

**Step 4: 테스트 완료 후 워크플로우 삭제 또는 보관**

---

## Task 2: Discord 봇 브릿지 스크립트 작성

디스코드 메시지를 n8n 웹훅으로 전달하는 경량 봇을 작성한다.

**Files:**
- Create: `discord-bot/index.js`
- Create: `discord-bot/package.json`
- Create: `discord-bot/.env.example`

**Step 1: package.json 작성**

```json
{
  "name": "fastfive-reservation-bot",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "discord.js": "^14.16.0",
    "dotenv": "^16.4.0"
  }
}
```

**Step 2: .env.example 작성**

```
DISCORD_TOKEN=your_discord_bot_token
DISCORD_CHANNEL_ID=your_channel_id
N8N_WEBHOOK_URL=http://your-n8n-host:5678/webhook/fastfive-reserve
```

**Step 3: index.js 작성**

```javascript
import 'dotenv/config';
import { Client, GatewayIntentBits } from 'discord.js';

const client = new Client({
  intents: [
    GatewayIntentBits.Guilds,
    GatewayIntentBits.GuildMessages,
    GatewayIntentBits.MessageContent,
  ],
});

const CHANNEL_ID = process.env.DISCORD_CHANNEL_ID;
const WEBHOOK_URL = process.env.N8N_WEBHOOK_URL;

client.on('ready', () => {
  console.log(`Bot ready: ${client.user.tag}`);
});

client.on('messageCreate', async (message) => {
  if (message.author.bot) return;
  if (message.channelId !== CHANNEL_ID) return;

  // 텍스트 메시지 또는 음성 첨부파일 처리
  const payload = {
    userId: message.author.id,
    username: message.author.username,
    channelId: message.channelId,
    messageId: message.id,
  };

  // 음성 첨부파일 확인
  const audioAttachment = message.attachments.find(
    (a) => a.contentType?.startsWith('audio/')
  );

  if (audioAttachment) {
    payload.type = 'voice';
    payload.audioUrl = audioAttachment.url;
  } else if (message.content.trim()) {
    payload.type = 'text';
    payload.text = message.content.trim();
  } else {
    return; // 빈 메시지 무시
  }

  try {
    await message.react('⏳');
    const res = await fetch(WEBHOOK_URL, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(payload),
    });
    const result = await res.json();

    await message.reply(result.message || '처리 완료');
    await message.reactions.cache.get('⏳')?.remove();
  } catch (err) {
    console.error('Webhook error:', err);
    await message.reply('❌ 예약 시스템에 연결할 수 없습니다.');
  }
});

client.login(process.env.DISCORD_TOKEN);
```

**Step 4: 의존성 설치 및 봇 실행 테스트**

Run: `cd discord-bot && npm install && node index.js`
Expected: `Bot ready: <봇이름>#<번호>`

**Step 5: 커밋**

```bash
git add discord-bot/
git commit -m "feat: Discord 봇 브릿지 스크립트 작성"
```

---

## Task 3: n8n 워크플로우 — 뼈대 생성

Discord 웹훅 수신 → 응답 반환까지의 기본 골격을 만든다.

**Step 1: 워크플로우 생성 (n8n MCP)**

`n8n_create_workflow`로 아래 구조의 워크플로우를 생성한다:

```
Webhook Trigger (POST /webhook/fastfive-reserve)
  → Is Voice?
    → [true] Download Audio → Whisper STT → Prepare Text → Claude Proxy
    → [false] Claude Proxy
  → Parse Reservation JSON
  → FastFive Login
  → Check Availability
  → Has Available Room?
    → [true] Create Reservation → Format Success Response
    → [false] Format Unavailable Response
  → Webhook Response
```

노드 구성:
1. **Webhook** — path: `fastfive-reserve`, method: POST, responseMode: `lastNode`
2. **Is Voice?** — `{{ $json.type === 'voice' ? 'yes' : 'no' }}` === 'yes'
3. **Download Audio** — GET `{{ $json.audioUrl }}`, response format: file
4. **Whisper STT** — POST `http://host.docker.internal:8100/v1/audio/transcriptions`
5. **Prepare Text** — 음성→텍스트 변환, 원본 payload 병합
6. **Claude Proxy** — POST `http://10.0.0.1:3456/v1/chat/completions`
7. **Parse Reservation JSON** — Code 노드, LLM 응답에서 JSON 추출
8. **FastFive Login** — POST `/auth/signin`
9. **Check Availability** — GET `/mobile/spaces/reservable`
10. **Has Available Room?** — IF 노드
11. **Create Reservation** — POST `/mobile/spaceReservations`
12. **Format Success/Unavailable Response** — Code 노드
13. **Webhook Response** — Respond to Webhook 노드

**Step 2: 워크플로우 검증**

Run: `n8n_validate_workflow`
Expected: 검증 통과

**Step 3: 커밋 (docs에 워크플로우 ID 기록)**

---

## Task 4: Claude Proxy — 예약 파싱 프롬프트

자연어 메시지에서 예약 정보(날짜, 시간, 인원)를 추출하는 LLM 프롬프트를 작성한다.

**Step 1: Claude Proxy 노드 설정**

```
POST http://10.0.0.1:3456/v1/chat/completions
Body:
{
  model: "claude-sonnet-4-6",
  messages: [
    {
      role: "system",
      content: "당신은 회의실 예약 파싱 도우미입니다. 사용자의 자연어 메시지에서 예약 정보를 추출하세요.\n\n현재 날짜: <$now KST>\n현재 요일: <$now 요일>\n현재 시간: <$now KST HH:mm>\n\n반드시 아래 JSON 형식으로만 응답하세요:\n{\n  \"date\": \"YYYY-MM-DD\",\n  \"startTime\": \"HH:mm\",\n  \"endTime\": \"HH:mm\",\n  \"capacity\": <숫자 또는 null>,\n  \"error\": false\n}\n\n규칙:\n- 30분 단위로 반올림 (예: 2:40 → 3:00, 2:10 → 2:00)\n- 종료 시간 없으면 시작 + 1시간\n- 인원 언급 없으면 capacity: null\n- 해석 불가능하면 { \"error\": true, \"message\": \"이해할 수 없는 요청입니다\" }"
    },
    { role: "user", content: <사용자 텍스트> }
  ],
  max_tokens: 300
}
```

**Step 2: Parse Reservation JSON 노드 구현**

```javascript
const aiResponse = $input.first().json.choices[0].message.content || '';
const jsonMatch = aiResponse.match(/\{[\s\S]*\}/);

if (!jsonMatch) {
  return [{ json: { error: true, message: '❌ 예약 정보를 인식할 수 없습니다.\n\n예시: "내일 오후 2시 회의실 예약해줘"' } }];
}

const parsed = JSON.parse(jsonMatch[0]);
if (parsed.error) {
  return [{ json: parsed }];
}

// ISO 형식 변환
parsed.startISO = parsed.date + 'T' + parsed.startTime + ':00+09:00';
parsed.endISO = parsed.date + 'T' + parsed.endTime + ':00+09:00';

return [{ json: { ...parsed, ...$('Webhook').item.json } }];
```

**Step 3: 테스트**

n8n webhook에 테스트 요청:
```json
{ "type": "text", "text": "내일 오후 2시에 4명 회의실 예약해줘" }
```
Expected: `{ "date": "2026-02-28", "startTime": "14:00", "endTime": "15:00", "capacity": 4, "error": false }`

**Step 4: 커밋**

---

## Task 5: FastFive API 체인 — 로그인 → 조회 → 예약

n8n 워크플로우에 FastFive API 호출 체인을 구현한다.

**Step 1: FastFive Login 노드**

```
POST https://api.fastfive.co.kr/auth/signin
Headers: X-Application: web
Body: { "application": "web", "email": "<credential>", "password": "<credential>" }
```

Note: n8n의 HTTP Request 노드에서 쿠키를 자동으로 전파하려면 `"batching"` 옵션 또는 Code 노드에서 Set-Cookie를 수동 파싱해야 할 수 있다. 대안으로 n8n의 HTTP Request 노드가 쿠키를 자동 전파하지 않는 경우, 로그인 응답의 Set-Cookie 헤더를 추출하여 이후 요청에 Cookie 헤더로 전달하는 Code 노드가 필요하다.

**Step 2: Extract Session Cookie 노드 (Code)**

```javascript
const headers = $input.first().json.$response?.headers || {};
const setCookie = headers['set-cookie'];
// 쿠키 문자열 추출
const cookies = Array.isArray(setCookie)
  ? setCookie.map(c => c.split(';')[0]).join('; ')
  : (setCookie || '').split(';')[0];
return [{ json: { ...$input.first().json, sessionCookie: cookies } }];
```

**Step 3: Check Availability 노드**

```
GET https://api.fastfive.co.kr/mobile/spaces/reservable
Headers:
  X-Application: web
  Cookie: {{ $json.sessionCookie }}
Query:
  spaceCategoryId: 7
  branchId: 90
  date: {{ $('Parse Reservation JSON').item.json.date }}
  startTime: {{ $('Parse Reservation JSON').item.json.startISO }}
  endTime: {{ $('Parse Reservation JSON').item.json.endISO }}
  sort: name
  order: asc
```

**Step 4: Filter Available Rooms (Code)**

```javascript
const spaces = $input.first().json; // API 응답
const capacity = $('Parse Reservation JSON').item.json.capacity;
let available = Array.isArray(spaces) ? spaces : (spaces.spaces || []);

// 인원 필터링 (capacity가 지정된 경우)
if (capacity) {
  available = available.filter(s => s.capacity >= capacity);
}

if (available.length === 0) {
  return [{ json: { hasRoom: false, message: '해당 시간에 예약 가능한 회의실이 없습니다.' } }];
}

// 가장 적합한 방 선택 (수용인원이 가장 작으면서 조건 충족)
available.sort((a, b) => a.capacity - b.capacity);
const selected = available[0];

return [{ json: {
  hasRoom: true,
  spaceId: selected.id,
  spaceName: selected.name,
  capacity: selected.capacity,
  floorName: selected.floorName
} }];
```

**Step 5: Has Available Room? (IF 노드)**

조건: `{{ $json.hasRoom }}` equals `true`

**Step 6: Create Reservation 노드**

```
POST https://api.fastfive.co.kr/mobile/spaceReservations
Headers:
  X-Application: web
  Content-Type: application/json
  Cookie: {{ $('Extract Session Cookie').item.json.sessionCookie }}
Body: {
  "spaceId": {{ $json.spaceId }},
  "startTime": "{{ $('Parse Reservation JSON').item.json.date }} {{ $('Parse Reservation JSON').item.json.startTime }}:00",
  "endTime": "{{ $('Parse Reservation JSON').item.json.date }} {{ $('Parse Reservation JSON').item.json.endTime }}:00",
  "memberGroupId": 351645
}
```

Note: startTime/endTime은 반드시 `"YYYY-MM-DD HH:mm:ss"` 형식 (ISO가 아님). memberGroupId: 351645 필수.

**Step 7: 커밋**

---

## Task 6: 응답 포매팅 및 Webhook Response

예약 결과를 사용자에게 알기 쉽게 전달한다.

**Step 1: Format Success Response (Code)**

```javascript
const reservation = $input.first().json;
const parsed = $('Parse Reservation JSON').item.json;
const space = $('Filter Available Rooms').item.json;

const msg = [
  '✅ 회의실 예약 완료!',
  '',
  `📍 ${space.spaceName} (${space.floorName}, ${space.capacity}인)`,
  `📅 ${parsed.date}`,
  `🕒 ${parsed.startTime} - ${parsed.endTime}`,
].join('\n');

return [{ json: { message: msg } }];
```

**Step 2: Format Unavailable Response (Code)**

```javascript
const parsed = $('Parse Reservation JSON').item.json;
const msg = [
  '❌ 예약 가능한 회의실이 없습니다.',
  '',
  `📅 ${parsed.date} ${parsed.startTime} - ${parsed.endTime}`,
  parsed.capacity ? `👥 ${parsed.capacity}인 이상` : '',
  '',
  '다른 시간을 시도해보세요.',
].filter(Boolean).join('\n');

return [{ json: { message: msg } }];
```

**Step 3: Webhook Response 노드**

Respond to Webhook 노드로 `{{ $json }}` 반환 (JSON).

**Step 4: 커밋**

---

## Task 7: 음성 입력 경로 (Whisper STT)

기존 "개인 일정 등록" 워크플로우의 음성 처리 패턴을 재활용한다.

**Step 1: Download Audio 노드**

```
GET {{ $json.audioUrl }}
Options: response format = file
```

**Step 2: Whisper STT 노드**

```
POST http://host.docker.internal:8100/v1/audio/transcriptions
Content-Type: multipart/form-data
Body:
  file: (binary data from Download Audio)
  model: Systran/faster-whisper-small
```

**Step 3: Prepare Text 노드 (Code)**

```javascript
const transcribedText = $input.first().json.text || '';
if (!transcribedText.trim()) {
  return [{ json: { error: true, message: '🎤 음성을 인식할 수 없습니다. 다시 시도해주세요.' } }];
}
return [{
  json: {
    ...$('Webhook').item.json,
    type: 'text',
    text: transcribedText
  }
}];
```

**Step 4: 커밋**

---

## Task 8: 에러 핸들링 경로

파싱 실패, API 오류, 로그인 실패 등의 에러를 처리한다.

**Step 1: Parse 에러 분기**

Parse Reservation JSON에서 `error: true`면 바로 Webhook Response로 보내는 경로 추가.

**Step 2: FastFive Login 실패 처리**

Login 노드에 `continueOnFail: true` 설정. 실패 시 에러 메시지 반환:
```
"❌ 패스트파이브 로그인에 실패했습니다. 관리자에게 문의하세요."
```

**Step 3: Reservation 생성 실패 처리**

Create Reservation 노드에 `continueOnFail: true` 설정. 실패 시:
```
"❌ 예약 생성에 실패했습니다. 이미 예약된 시간이거나 크레딧이 부족할 수 있습니다."
```

**Step 4: 커밋**

---

## Task 9: 통합 테스트 및 활성화

**Step 1: n8n 워크플로우 검증**

Run: `n8n_validate_workflow`
Expected: 통과

**Step 2: 수동 웹훅 테스트**

```bash
curl -X POST http://<n8n-host>:5678/webhook-test/fastfive-reserve \
  -H "Content-Type: application/json" \
  -d '{"type":"text","text":"내일 오후 3시 4명 회의실","userId":"test","channelId":"test"}'
```

Expected: 예약 성공 또는 가용 여부 피드백 메시지 반환

**Step 3: Discord 봇 연동 테스트**

1. Discord 봇 실행
2. 지정 채널에서 "내일 오후 3시 회의실 예약해줘" 메시지 전송
3. 봇이 ⏳ 반응 후 예약 결과 응답 확인

**Step 4: 워크플로우 활성화**

```
n8n MCP: activateWorkflow
```

**Step 5: Discord 봇을 백그라운드 서비스로 등록**

pm2 또는 systemd로 Discord 봇을 데몬화:
```bash
npm install -g pm2
cd discord-bot && pm2 start index.js --name fastfive-bot
pm2 save
```

**Step 6: 최종 커밋**

```bash
git commit -m "feat: 패스트파이브 회의실 디스코드 예약 봇 구현 완료"
```

---

## n8n 워크플로우

- **워크플로우 ID:** `xsaud3O5rFEs9L7J`
- **워크플로우 이름:** 패스트파이브 회의실 예약
- **노드 수:** 19개
- **상태:** ✅ 활성 (통합 테스트 통과)

### 워크플로우 흐름
```
Webhook → Is Voice?
  → [voice] Download Audio → Whisper STT → Prepare Text → Build Claude Request
  → [text] Build Claude Request
→ Claude Proxy → Parse Reservation JSON → Has Parse Error?
  → [error] Format Error → Respond to Webhook
  → [ok] FastFive Login → Extract Cookie → Check Availability → Pick Room → Has Available Room?
    → [yes] Create Reservation → Format Reservation Result → Respond to Webhook
    → [no] Format Unavailable → Respond to Webhook
```

---

## 통합 테스트 결과

### 테스트 1: 텍스트 예약 (성공)
```
입력: "내일 오후 3시 4명 회의실"
결과: ✅ 회의실 예약 완료! 📍 7B (7, 4인) 📅 2026-02-28 🕒 15:00 - 16:00
```

### 발견된 버그 및 수정
1. **Webhook 404 (not registered)** — 원인: Webhook 노드에 `webhookId` 프로퍼티 누락. `webhookId: "fastfive-reserve"` 추가하여 해결.
2. **Claude "이해할 수 없는 요청입니다" 반환** — 원인: Build Claude Request에서 `$input.first().json.text` 접근했으나, Webhook 데이터는 `body.text`에 위치. `body?.text || text` 폴백 패턴으로 수정.

### 환경 설정
- n8n 원격 인스턴스: `https://unu.unuuuuu.com`
- Discord 봇 .env: `N8N_WEBHOOK_URL=https://unu.unuuuuu.com/webhook/fastfive-reserve`

---

## 알려진 리스크 및 미해결 사항

1. ~~**FastFive API request body 형식**~~ — ✅ Task 1에서 검증 완료. body: `{spaceId, startTime, endTime, memberGroupId}`.
2. ~~**쿠키 전파**~~ — ✅ Extract Cookie Code 노드로 Set-Cookie → Cookie 헤더 수동 전달 구현 완료.
3. **세션 만료** — FastFive 세션이 만료되면 재로그인 필요. 현재 계획은 매 요청마다 로그인하는 방식 (해결됨).
4. **크레딧** — 30분 = 1크레딧. 예약 생성 시 자동 차감, 취소 시 `returnedCredits`로 반환. 크레딧 잔고 부족 시 에러 처리 필요.
5. **Discord 봇 권한** — Message Content Intent 활성화 필요 (Discord Developer Portal).
6. **고아 예약 2건** — Task 1 테스트 중 `memberGroupId` 없이 생성된 예약 (id: 3695528, 3695529). API로 취소 불가. 웹에서 수동 확인 필요.
